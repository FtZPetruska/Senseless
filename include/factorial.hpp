#include <cstdint>

namespace MyProject {

/**
 * @brief Calculate a factorial.
 *
 * @param n the number to get the factorial of.
 * @return n! or 0 if overflow.
 */
std::uint64_t factorial(std::uint8_t n);

}; // namespace MyProject
